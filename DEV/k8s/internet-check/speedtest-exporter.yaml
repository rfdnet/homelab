apiVersion: apps/v1
kind: Deployment
metadata:
  name: speedtest-exporter
  namespace: internet-check-home
  labels:
    app.kubernetes.io/name: speedtest-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: speedtest-exporter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: speedtest-exporter
        app: speedtest-exporter  # <-- Adicione esta linha para corresponder ao Service
    spec:
      containers:
        - name: speedtest-exporter
          image: miguelndecarvalho/speedtest-exporter:latest
          ports:
            - containerPort: 9798
              name: metrics
